#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn (ros:ensure-asdf))

(defpackage :ros.script.atty
  (:use :cl))
(in-package :ros.script.atty)

(asdf:load-system :atty :verbose nil)

(defun main (&rest argv)
  (atty:main argv))
